Define HashMap<String, Integer> studentScores

Insert ("Alice", 80)
Insert ("Bob", 92)
Insert ("Charlie", 88)

Find highest scorer using stream:
    maxEntry = studentScores.entrySet().stream()
                 .max(by value)
    print maxEntry.key + " scored highest: " + maxEntry.value
